class BuildConfig {
  BuildConfig({
    this.projectName,
    this.projectLocation,
    this.description,
    this.packageName,
  });

  String? projectName;
  String? projectLocation;
  String? description;
  String? packageName;
}
